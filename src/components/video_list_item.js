import React from 'react';

const VideoListItem = ({video}) => {

  return (
    <li className="collection-item avatar">
      <i className="material-icons circle blue">play_arrow</i>
    </li>

  )
};

export default VideoListItem;
